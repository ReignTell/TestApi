handlers.helloWorld = function(args, context) {
    log.info("HelloWorld function called");  // Log for debugging

    // Ensure the return value is correctly formatted and contains expected fields
    return { message: "Hello World from Cloud Script!" };
};
